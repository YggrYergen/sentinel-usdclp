# T0.9-B -- progreso (implementador, overnight grid: failure isolation + parallelism + progress)

- Leido: CHARTER.md, protocolos/06-runners.md, protocolos/04-enmiendas-trazabilidad.md parte 3,
  LEDGER.schema.md, y el codigo existente (runner.py, state.py, manifest.py, ledger.py,
  lineage.py, tasks.py, tasks_ticks.py, tasks_ticks_csv.py, test_runner_scaffold.py).
- Flag detectado en el spec: SS2 dice "tasks_ticks.py y tasks_ticks_csv.py registran tipos
  MT5-backed" pero tasks_ticks_csv.py es un ingest de CSV exportado a mano -- nunca importa el
  paquete MT5 (su propio self-check lo prueba). La instruccion concreta ("marca esos dos tipos")
  es inequivoca por nombre de archivo/tipo, asi que se implementa tal cual (ambos
  parallelizable=False); discrepancia documentada en el codigo (comentario junto al register()
  en tasks_ticks_csv.py) y en el reporte final para el controlador. No bloqueante -- no requirio
  detener la tarea.
- tasks.py: agregado kwarg `parallelizable` a `register()` + `get_non_parallelizable_types()`.
- tasks_ticks.py / tasks_ticks_csv.py: marcados `parallelizable=False` en su `register()`.
- state.py: agregado `failed` dict + `durations` dict junto a `completed` (sin cambiar la forma
  de `completed[run_key]`). `mark_failed()` incrementa `attempts`. `mark_complete()` promueve
  fuera de `failed` (retry semantics). `is_complete()` sigue devolviendo False para claves
  fallidas -- sin cambios de comportamiento.
- Nuevo `progreso.py`: escritor atomico (tmp+replace) de `_progreso.json` / `_progreso.txt`, hora
  local del host (nunca utcnow(), nunca offset).
- Nuevo `progress.py`: CLI `python -m scripts.research.runner.progress <resultados_dir>` --
  imprime `_progreso.txt` o un mensaje claro si no existe.
- runner.py reescrito: `--on-error {abort,continue}` (default abort, path idéntico al anterior
  incluyendo el mensaje de RunnerAbort), `--workers N` (default 1). Unifica ejecucion
  secuencial/paralela en `_run_task()` (nunca lanza, siempre retorna dict ok/fallo +
  duration_s) + `_record_result()` (solo corre en el proceso padre: LEDGER, state, resumen,
  progreso -- en orden de finalizacion de future). Guard MT5: fuerza --workers a 1 e imprime por
  que cuando el manifiesto incluye un tipo `parallelizable=False`.
- TDD: tests/research/test_runner_overnight.py escrito primero (rojo confirmado: 7/7 fallaban
  por flags CLI inexistentes), luego implementacion, luego verde 7/7.
- Regresion: tests/research/test_runner_scaffold.py sin modificar, 5/5 verde. tests/research
  completo: 279 passed, 4 deselected (marca `slow`), 0 rojos nuevos. Confirmado estable en 5
  corridas repetidas de test_runner_overnight.py y 2 corridas repetidas de la suite completa.
- Hallazgo de plataforma (no bug del runner): `time.sleep(0.01)` en este host Windows retorna en
  ocasiones en ~0s (confirmado con un script standalone fuera del runner, reproducible ~30% de
  las veces). El test de duraciones positivas (#6) usa un busy-wait sobre `time.monotonic()` en
  vez de `time.sleep()` para no depender de esa cuirk de temporizador del SO.
- Commit del codigo: solo `scripts/research/runner` + `tests/research/test_runner_overnight.py`
  (git add -- scoped, verificado con git status antes/despues).
