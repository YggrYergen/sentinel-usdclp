# T0.7-M-C — Bitácora de progreso (para retomada en frío)

## Estado: EN CURSO

## Lo ya establecido (lectura de código, sin ejecutar nada todavía)

1. `first_at` citado verbatim en `scripts/analysis/realtick_bt/backtest.py:130-139`.
   Búsqueda hacia adelante SIN COTA salvo por los 3 meses candidatos
   (`_candidates`: mes-1, mes, mes+1 relativos al mes del propio `t_sec`
   pedido). Si no hay tick `>= t_sec` en ninguno de esos 3 ficheros, `None`.
   Si lo hay, lo devuelve sin comprobar distancia.

2. Las dos llamadas del harness:
   - `backtest.py:353` (`resolve()`, entrada): `tc = bar_times[bi] + BAR_SEC`,
     dentro de un bucle `for bi in range(lo, max(lo+1, hi))` acotado por
     `MAX_RETRY_BARS=1` y por `if tc >= pos["t_out"] + BAR_SEC: break`.
     Tras `e = ticks.first_at(tc)`, NO hay ningún chequeo de
     `e[0] - tc`; se usa `ebid`/`eask` directamente para `sp = eask-ebid` y,
     si pasa el gate 0.5, para `entry_fill`.
   - `backtest.py:383` (`resolve()`, salida fallback): `t_out_close =
     pos["t_out"] + BAR_SEC`. `x = ticks.first_at(t_out_close)`. Tampoco hay
     chequeo de distancia; `xbid`/`xask` se usan directamente como
     `exit_fill`.
   Ambas llamadas pasan SIEMPRE un argumento de la forma "límite de vela M15
   + BAR_SEC" (un cierre de vela).

3. Barrido repo-wide (fuera de `faulty/` y `data/analysis/p_cap/`):
   - `scripts/analysis/a6_pata_a/signal_level.py:90-133` — reimplementación
     PROPIA e independiente de `Ticks`/`first_at` (mismo patrón, mismo bug,
     pero NO es la clase del harness).
   - `scripts/analysis/a6_pata_a/capa3_gate_spread.py:36-38,111-168` —
     IMPORTA `Ticks`/`resolve` directamente de `backtest.py` (read-only),
     mismas dos llamadas replicadas para instrumentación de un análisis A6.
   - `scripts/analysis/a6_pata_a/exp_active_fichas.py:195`,
     `capa1_senal_cruda.py:389` — instancian `Ticks()` (a confirmar de
     dónde importan).
   - `tests/analysis/test_realtick_pairing.py:61-78` — otra reimplementación
     propia en un fixture de test.
   - `scripts/research/backtest_largo_ava.py` — **ESTE ES "el motor de
     backtest largo"**: `AvaTicks(bt.Ticks)` sobrescribe SOLO `_load`;
     `first_at`/`range`/`_candidates` se heredan BYTE-IDÉNTICOS (documentado
     en el docstring del propio módulo, líneas 10-16). Usa `bt.build_all()`
     directamente. Produjo `F0-BT-LARGO-0001` (ledger, git_sha `86166f1`,
     rango de barras `2022-01-02 23:00:00` → `2026-08-12 04:15:00`, holdout
     2023 excluido, `n_barras_tras_exclusiones_d33=31700`).
   - `sentinel_engine/*` (`dossier.py`, `fast_replay.py`,
     `service/routers/runs.py`) — falsos positivos: usan
     `DataFrame.Index.searchsorted` para lookups "as-of" hacia ATRÁS sobre
     barras (`side="right") - 1`), NO son accesos a ticks por instante hacia
     adelante. Descartados.

4. AMBIGÜEDAD DECLARADA (pregunta 4/5): `_bars_M15.parquet` (sustrato de
   barras del harness Capitaria) solo cubre hasta **2026-07-24 16:45:00**,
   ANTES de la ventana pedida `2026-07-27 → 2026-08-11`. El harness NO PUEDE
   generar posiciones reales (ENTRY/EXIT) en esa ventana por falta de barras.
   REJILLA DECLARADA: dado que ambos call sites (`:353`, `:383`) siempre
   construyen el argumento como "límite de vela M15 + BAR_SEC" (cierre de
   vela), uso como rejilla TODOS los cierres de vela M15 (múltiplos de 900s)
   que caen en `[2026-07-27 00:00:00, 2026-08-12 00:00:00)` hora servidor, y
   les aplico `ticks.first_at()` directamente contra el sustrato real de
   Capitaria (`data/lake_ticks/XAUUSD/*.parquet`, filtrado `^[0-9]{6}$`).
   Motivo: reproduce la FORMA exacta del argumento sin necesitar posiciones
   reales que no existen por falta de barras.

5. Para Q6 (AVA / `F0-BT-LARGO-0001`): en vez de una rejilla aproximada,
   plan es INSTRUMENTAR `AvaTicks` (subclase que registra cada `t_sec`
   pedido a `first_at` antes de delegar a `super()`) y correr
   `bt.build_all()` sobre `bars_final` reales (mismas funciones que
   `backtest_largo_ava.py` usa: `load_bars_ava`, `vc.filter_bars`,
   `filter_exclusiones`) — reproduce las llamadas GENUINAS, no una rejilla
   aproximada. Cruce con `exclusiones-ava.json` por overlap directo de
   intervalos.

## Siguiente paso
Escribir `tests/analysis/test_lookahead_harness.py` (rojo), luego
`scripts/analysis/realtick_bt/auditoria_lookahead.py` (mínimo, verde),
correr medición real, escribir artefactos en
`research/fases/F0-preparacion/04-resultados/T0.7-p-cap/auditoria_lookahead.*`.

## Commits de esta tarea
(ninguno todavía)
