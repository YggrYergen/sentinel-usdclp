# T0.7-M-0 — bitácora de progreso (resiliencia de sesión)

Última actualización: inicio de sesión, tras lectura de brief/CHARTER/TRACKER y exploración de datos.

## Estado

- [x] Leído brief, CHARTER, TRACKER.
- [x] Explorados los ficheros fuente: `data/analysis/p_cap/replica/posiciones_replica.csv` (157 filas,
      columnas: strategy_id, ficha, side[L/S], t_open, t_open_servidor, precio_open,
      sl_open_deseado, sl_open_enviado, clamp_aplicado, t_close, t_close_servidor, precio_close,
      motivo_cierre, sl_vivo. Sin NaN. Sin `position_id`.) y
      `data/analysis/p_cap/comparacion_p_cap.csv` (162 filas = 152 REAL + 10 REPLICA_SIN_PAREJA,
      47 columnas, ya trae `replica_*` emparejado por el comparador anterior).
- [x] Confirmado `side` réplica: `L`=BUY / `S`=SELL, verificado 1:1 contra `real_side` en las 157
      posiciones emparejadas (0 discrepancias, crosstab 78 BUY-L / 69 SELL-S).
- [x] Control del punto 2 ejecutado EN EXPLORACIÓN (no aún en el instrumento definitivo): 0
      posiciones con `bruto_usd==0`; tasa implícita limpia por día (dispersión relativa diaria
      0,03%–0,68%, dispersión global 0,93%). **Control PASA** — la fórmula es correcta.
- [x] Discrepancia detectada entre el brief y el artefacto (regla de resiliencia: gana el
      artefacto): el brief (prosa, líneas 33-36 y línea 117) dice "137" posiciones emparejadas /
      "128 de 137" / "131 de 137"; pero `p_cap_resultado.json` (`criterio_de_paso_6_campos`) dice
      **135** emparejadas evaluables (`razon_cierre`: 128 sí + 6 no + 1 no_evaluable = 135;
      `resultado`: 131 sí + 4 no = 135). Verificado también recomputando directamente de
      `comparacion_p_cap.csv`: 140 evaluables (excluido_criterio==False) − 5 sin pareja = 135.
      **Uso 135, con la discrepancia declarada en el reporte.**
- [x] Definidas las 3 poblaciones de la pregunta 5 a partir de los datos:
      (a) 135 = emparejadas Y evaluables (excluido_criterio==False, con réplica).
      (b) 152 real (todas, sin excluir) vs 157 réplica (todas), sumas independientes.
      (c) 147 = matched (con réplica) sin filtrar por excluido_criterio, en ambos lados
      (147 = 157 réplica − 10 replica_sin_pareja = 152 real − 5 real_sin_pareja).
- [x] Descomposición (pregunta 6) verificada numéricamente en exploración: los tres sumandos
      (bruto de los 10 replica_sin_pareja, −bruto de los 5 real_sin_pareja, diff de los 147
      emparejados) reconstruyen el diff de la población (b) con residuo ~1.8e-12 (ruido de punto
      flotante, no error).
- [x] Tests escritos (rojo confirmado: `ImportError` al importar el módulo inexistente).
- [x] Implementación mínima escrita. Verde: `pytest tests/analysis/test_divergencia_neto.py -q`
      → 21 passed en la primera pasada.
- [x] **Bug real encontrado al correr el instrumento sobre datos reales** (no lo atrapó el test
      unitario inicial porque usaba fixtures pequeñas con índice contiguo): en `_preparar_real()`,
      `tasa_implicita(...)` devuelve una Series con índice nuevo (RangeIndex 0..n-1), pero `real`
      (filtrado de `cmp`) conserva su índice ORIGINAL no contiguo (10 filas `REPLICA_SIN_PAREJA`
      intercaladas lo rompen). La asignación `real["tasa"] = tasa_implicita(...)` alineó por
      ÍNDICE, no por posición, y coló 10 NaN silenciosos (n pasó de 152 a 142 en el control).
      Corregido forzando `.to_numpy()` en la asignación. Añadido test de regresión
      `test_tasa_implicita_asignable_a_dataframe_con_indice_no_contiguo`. Verificado tras el fix:
      `tasa_stats_global.n` = 152 (antes 142), `tasa_dia_no_evaluable_poblacion_a/b` = 0/0 (antes 6/7).
- [x] **Segundo bug encontrado por inspección del CSV de salida:** `poblacion_c_matched` incluía
      las 10 filas `REPLICA_SIN_PAREJA` (tienen `replica_side` poblado -- es su propio lado -- pero
      NO son un par real-réplica por definición). Sumaba 157 en vez de 147. Corregido restringiendo
      el flag a `tipo_fila=='REAL' & replica_side.notna()`. Test de regresión
      `test_csv_poblacion_c_excluye_replica_sin_pareja`. No afectaba los números agregados del JSON
      (que ya usaban la definición correcta internamente vía `_preparar_matched`), sólo la columna
      de conveniencia del CSV posición-a-posición.
- [x] También eliminado código muerto sin usar (`_divergencia_poblacion`, `_construir_fila_csv`).
- [x] **Todos los números del control (§2) y de la divergencia (§3-§6) verificados de forma
      independiente** en exploración manual con pandas antes de escribir el instrumento (mismo
      resultado exacto tras el fix del bug de índice: p.ej. divergencia_pct población (a) USD =
      17.270344015837768, decomposición residuo ~1.8e-12).
- [x] Instrumento corrido con datos reales, artefactos escritos y verificados.
- [x] `pytest tests/analysis -q` re-corrido tras todos los cambios → **268 passed, 0 failed**
      (incluye 23 tests propios + los de otros agentes trabajando en paralelo sobre otros
      ficheros de `tests/analysis/`, fuera de mi alcance).
- [x] Los 4 artefactos existen en sus rutas exactas del brief.
- [x] Ficheros `.bak-*` intermedios (de mis propias 3 corridas de depuración dentro de esta misma
      sesión, no de una sesión anterior) borrados tras confirmar que la corrida final es correcta
      -- no eran parte del entregable y no había nada previo que proteger.
- [ ] **Siguiente paso:** commit scoped, escribir `T0.7-M-0-reporte.md` (contrato de reporte del
      orquestador), reportar estado final.

## Ficheros que se van a escribir (rutas exactas del brief)

- `scripts/analysis/realtick_bt/faulty/divergencia_neto.py`
- `tests/analysis/test_divergencia_neto.py`
- `research/fases/F0-preparacion/04-resultados/T0.7-p-cap/divergencia_neto.csv`
- `research/fases/F0-preparacion/04-resultados/T0.7-p-cap/divergencia_neto.json` + `.md`

## Baseline verificada antes de empezar

`pytest tests/analysis -q` → **214 passed, 0 failed** (foreground, 72.03s). Coincide con la línea
base declarada en el brief.
