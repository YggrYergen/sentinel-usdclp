# BRIEF — T0.7-M-H · Re-medición del neto con el coste de deslizamiento de los stops calibrado

**Rol:** IMPLEMENTADOR + medición REPORT-ONLY (Sonnet 5 high effort). **Rama:** `equipo1`. **Fecha:** 2026-08-16
**Manda:** **D-54** (`research/DECISIONES.md`) — el deslizamiento de los cierres por stop sale del
criterio de paridad y entra como **coste modelado calibrado con datos propios ya en disco**.
**Bitácora obligatoria:** `research/fases/F0-preparacion/02-specs/T0.7-M-H-progreso.md` — UNA línea
por bloque, escrita al terminar el bloque, antes del commit.
**Reporte final (uno solo):** `research/fases/F0-preparacion/02-specs/T0.7-M-H-reporte.md`.

---

## 0 · El hecho que hace falta medir

A6 comparó el simulador contra la ventana real de la cuenta 902 (2 semanas, ~152 posiciones). Estado
actual del artefacto `04-resultados/T0.7-p-cap/divergencia_neto.json` (generado hoy tras el fix
T0.7-M-G, `git_sha 0198002`):

- Divergencia de neto sobre las **134** emparejadas evaluables: **16,72 %** (real −12.453,29 USD ·
  réplica −10.371,60 USD).
- 🔴 **El sesgo es DIFERENCIAL y de signo opuesto:** S6 **−5,13 USD/posición** (n=79) ·
  SuperTrend **+45,22 USD/posición** (n=55). Cociente 8,81×. El simulador **embellece** justo a la
  estrategia que en la realidad perdió dinero (ST, −16.483,34 USD reales).

De los **119** cierres por stop server-side, **cero** coinciden con la cotización vigente; los **21**
cierres a mercado casan al céntimo. La sospecha que hay que **medir** (no asumir): si ST cierra por
stop con más frecuencia que S6, un coste de deslizamiento mal modelado es candidato directo a
explicar ese embellecimiento diferencial.

**Tú mides. No interpretas.** Reportas números, rutas, conteos y los comandos con su salida real.
Prohibido concluir, recomendar o priorizar (charter §B, rol INVESTIGADOR).

---

## 1 · Recon obligatorio (Bloque 0)

Diez líneas en la bitácora confirmando dónde vive de verdad cada cosa. **Donde este brief y el
código difieran, gana el código**, y lo dices en el reporte.

Punto de partida verificado por el controlador el 2026-08-16:
- Generador: `scripts/analysis/realtick_bt/faulty/divergencia_neto.py` (721 líneas, pandas puro
  sobre CSVs; **no importa el motor**).
- Entradas: `posiciones_replica.csv` y `comparacion_p_cap.csv` (rutas resueltas dentro del script).
- Salidas actuales: `04-resultados/T0.7-p-cap/divergencia_neto.{csv,json,md}`.
- Datos de fill: `04-resultados/T0.7-p-cap/fill_vs_cotizacion.{csv,json,md}`, generado por
  `scripts/analysis/realtick_bt/faulty/fill_vs_cotizacion.py`. 🔴 **Dato de método ya verificado y
  que debes tener presente:** en `fill_vs_cotizacion.py:69-77` la referencia contra la que se mide es
  **la cotización al PRINCIPIO del segundo**, y un stop se dispara por construcción durante un
  movimiento en contra ⇒ **parte del desvío medido es artefacto del método**, no dinero perdido.
  Decláralo en el reporte como límite de la calibración; no lo corrijas (esa corrección es la tarea
  `T0.7-M-A`, deliberadamente diferida por D-54).

---

## 2 · Bloques

### Bloque 1 — Calibrar el coste con código, no con las cifras de este brief
D-54 cita, de mediciones previas: desvío mediano **0,255** con spread 0,50 (n=110) y **0,270** con
spread 0,60 (n=9) en los cierres por stop, contra **0,053** en los 21 cierres a mercado.
🔴 **Ésas son lecturas del controlador: recalcúlalas tú desde `fill_vs_cotizacion.csv` y reporta tus
propios números.** Si no coinciden, **gana tu recálculo** y lo dices. Todo número lo calcula código.

Modelo de coste: `deslizamiento(spread) = mediana|delta|(stops, ese spread) −
mediana|delta|(cierres a mercado)`. Con los valores citados: 0,202 a spread 0,50 y 0,217 a 0,60.
Si el spread del instante de cierre no es recuperable para alguna posición, usa el modo dominante
0,50 y **cuenta cuántas** cayeron en ese caso.

### Bloque 2 — Aplicar el coste a la réplica y re-medir
Aplica el deslizamiento **en contra** de la posición, sólo a los cierres por stop de la réplica
(LONG: precio de cierre más bajo; SHORT: más alto). Los cierres a mercado no se tocan.
Recomputa las mismas magnitudes que el artefacto actual: divergencia global, divergencia por
estrategia, sesgo por posición y por lote, y la descomposición.

🔴 **NO sobrescribas los artefactos existentes.** Escribe ficheros NUEVOS:
`04-resultados/T0.7-p-cap/divergencia_neto_con_deslizamiento.{csv,json,md}`, con el bloque de
linaje completo (`run_id`, `area`, `experimento`, `substrate_id`, `engine_sha`, `git_sha`, `etapa`,
`generador`, `timestamp`) igual que el artefacto actual. Registro **aditivo**: jamás borrar ni mutar
filas de resultados. Si aun así regeneras algo existente, respaldo `<nombre>.bak-<timestamp UTC>`
primero.

Implementación preferida: **flag nuevo apagado por defecto** en `divergencia_neto.py` (p. ej.
`--coste-deslizamiento`), de modo que sin el flag el script produzca **exactamente** lo de hoy.
Verifícalo: corre sin flag y compara contra el artefacto actual campo a campo.

### Bloque 3 — Los tres números que decidirán qué se puede concluir
Reporta, con código:
1. Divergencia global **antes** y **después**: 16,72 % → ?
2. Sesgo por posición **antes** y **después**, por estrategia: S6 −5,13 → ? · ST +45,22 → ?
   ¿El cociente 8,81× se estrecha, se mantiene o se ensancha?
3. **Frecuencia y peso de los cierres por stop por estrategia**: nº y % de cierres por stop sobre el
   total de cada estrategia, en la realidad y en la réplica, más USD afectados. Es el dato que dice
   si el mecanismo es siquiera capaz de explicar el sesgo diferencial.

Añade el control obvio: si aplicaras el mismo coste a **todas** las posiciones por igual, ¿cuánto se
movería el sesgo diferencial? Sirve para separar "el coste importa" de "el coste importa **de forma
asimétrica entre estrategias**".

---

## 3 · Fuera de alcance

- **No corras `T0.7-M-A`** (precio de cierre contra el nivel del stop): está diferida por D-54.
- No toques el motor, ni `backtest.py`, ni las estrategias, ni el harness real-tick.
- No escribas en `research/LEDGER.jsonl`, `research/TRACKER.md`, `research/DECISIONES.md` ni en el
  plan maestro: **son del CONTROLADOR**. Deja los datos de linaje en tu reporte y él escribe la fila.
- No interpretes. Ni una recomendación, ni un veredicto.

---

## 4 · Criterios de aceptación (definición de HECHO)

1. Sin el flag nuevo, `divergencia_neto.py` reproduce **exactamente** el artefacto de hoy
   (verificado campo a campo por código, no a ojo).
2. Los artefactos `divergencia_neto_con_deslizamiento.{csv,json,md}` existen, con linaje completo.
3. `python -m pytest tests/analysis -q` sin regresiones respecto de la línea de partida que midas tú
   mismo en el Bloque 0 (mídela y anótala). Pytest **siempre en primer plano**.
4. El reporte trae los comandos ejecutados **con su salida real**, y lo que no pudiste hacer y por
   qué.

---

## 5 · §B ROUTING — bloque VERBATIM del charter

> **Routing de modelos del programa (70 / 29 / 1):**
>
> **Sonnet 5 high effort ≈ 70 %** — el caballo de batalla. Dos roles, nunca mezclados:
> · **IMPLEMENTADOR**: recibe un spec técnico CERRADO y detallado (qué implementar, dónde,
>   con qué cuidados, pointers a ficheros y contexto relevante). Implementa. No decide diseño.
> · **INVESTIGADOR**: recopila y reporta **objetivamente** — números, rutas, conteos, citas
>   `file:line`. **REPORT-ONLY: prohibido interpretar, recomendar, concluir o priorizar.**
> A Sonnet **NUNCA** se le pide interpretar resultados, hacer recomendaciones ni tomar decisiones.
> Siempre se le despacha con **información completa** (no hereda contexto).
>
> **Opus 5 high effort ≤ 29 %** — orquestación e inteligencia. **Máximo 2 subagentes en paralelo**,
> y NUNCA dos sobre los mismos ficheros. Opus hace: toda interpretación de resultados, propuesta
> de mejoras, generación de ideas, redacción de specs y memos, decisiones de diseño.
> **Toda etapa de ANÁLISIS DE RESULTADOS usa Opus, siempre.**
>
> **Fable 5 ≈ 1 %** — reservado a 3 momentos de máximo apalancamiento:
> (1) cierre e integración del plan + charter; (2) auditoría pre-vuelo del motor congelado y de
> los resultados de A6, antes de autorizar la ejecución; (3) síntesis final del programa.
>
> Ficheros compartidos (TRACKER, LEDGER, planes, `.gitignore`) son del **CONTROLADOR**,
> jamás de un agente.

## 6 · §C NORMAS DE PROCESO — bloque VERBATIM del charter

> **R1-bis:** ver §A.11 — vivos byte-idénticos, todo sobre copias, PARAR y escalar si crees que
> hay que tocar el original.
>
> **Paralelismo:** máximo **2** subagentes simultáneos. NUNCA dos sobre los mismos ficheros —
> ni siquiera lectura de un fichero que otro está editando.
>
> **Git:** commitear SOLO las rutas propias: `git add -- <rutas>` y
> `git commit -m "<msg>" -- <rutas>` (el `-m` va ANTES del `--`). Verificar `git status --short`
> antes y después. Ni `git add .`, ni `-A`, ni rebase, ni push, ni tags.
>
> **Pytest:** PROHIBIDO en background. Foreground con timeout largo. Suites dirigidas
> (`tests/analysis`, `tests/live`), no la suite completa: hay 13 rojos conocidos por fuga de
> variables de entorno del host vivo (deuda de test, no bugs), y los lentos están en cuarentena
> con marca `slow`.
>
> **MT5:** ver §A.12 — attach-only, reales read-only, 902 solo lectura, `assert_demo()`.
>
> **Honestidad:** todo número lo calcula código; lo no evaluable se declara no evaluable; registro
> aditivo (jamás borrar ni mutar filas de resultados); veredictos solo bajo la puerta estadística
> del plan (§9). Ningún artefacto de datos lleva conclusiones.
>
> **Reporte:** al terminar, entrega (a) rutas exactas de los artefactos producidos, (b) los
> comandos ejecutados y su salida real, (c) lo que NO pudiste hacer y por qué. Sin adjetivos.

## 7 · Normas específicas de este programa

- Rama `equipo1`. **NUNCA** `master` ni `alvaro`. Sin force-push, rebase, amend, push ni tags.
- Todos los timestamps son **hora de servidor del bróker (UTC−4)**. **Prohibida toda conversión de
  zona.** Esta regla ya costó dinero real.
- La verdad de terreno tiene **resolución de segundo entero** (D-46).
- `pandas` convierte `None` en `NaN` y `is not None` no lo ve — usa `pd.isna()`.
- El spread de Capitaria es **bimodal 0,50 / 0,60**, no fijo; el spread por operación **no se
  registra hoy**.
- `eventos_ejecutor_902.csv` no trae la estrategia en `SENT MODIFY`, `SENT CLOSE`, `SL_CLAMPED` ni
  `FALLBACK_CLOSE_INVALID_SL` — se empareja por ticket.
- Los cierres por SL/TP de MT5 llegan con `magic=0`: la estrategia se hereda por `position_id`, no
  por magic.
- `scripts/analysis/a6_pata_a/` está **sin trackear**: `git add` sobre el directorio lo incorpora
  entero. Añade por ruta explícita, siempre.
- **D-48, disciplina de salida:** un fichero de reporte, bitácora de una línea por bloque, mensaje
  final corto — estado, SHAs, una línea de tests, y sólo las dudas que exijan decisión humana.
- Commit por **cada** incremento verde: cuatro agentes han muerto por límite de sesión en este
  programa.
- 🔴 **Hay otro agente trabajando en paralelo** sobre `scripts/analysis/realtick_bt/backtest.py`,
  `sentinel_engine/strategies/emasar_*.py` y `tests/research/`. **No leas ni toques esas rutas.**
  Las tuyas son: `scripts/analysis/realtick_bt/faulty/divergencia_neto.py`,
  `research/fases/F0-preparacion/04-resultados/T0.7-p-cap/` y tus dos ficheros de `02-specs/`.
