# T0.7-M-G — bitácora de progreso

- Leído CHARTER.md, protocolo 00, T0.7-M-B1-reporte.md, T0.7-M-D-reporte.md,
  `ciclos.py`, `llamador.py`, `backtest.py::Ticks`.
- Identificados con `comparacion_p_cap.csv` (estado post T0.7-M-D, sin tocar nada):
  18 posiciones reales a las 18:45 → 9 correctas (delta 0-4s), 5 tempranas
  (~17:59:5x, delta ~2706-2715s), 4 fuera de alcance (2 sin pareja, 2 casos raros
  20:27 y 23:25).
- Verificado contra el lago de ticks real (`data/lake_ticks/XAUUSD/*.parquet`) el
  spread real en los 3 instantes distintos que cubren las 5 posiciones tempranas
  (2026-07-30, 2026-08-02, 2026-08-10): el primer tick tras el hueco de mercado
  llega ~5-6s después de 18:00:00 con spread EXACTO 0.50 en los tres casos —
  spread real NO habría rechazado (evidencia contra H2 para estos 5 casos).
- Mecanismo confirmado: `ciclos.py` paso 3 evalúa `_seconds_of_day(t)` (instante
  sintético del bucle) en vez de `_seconds_of_day(_tick_ts)` (timestamp real del
  tick que `first_at(t)` devolvió, dentro de tolerancia ~13s < cycle_sec=15.0).
  `t` cae antes de 18:00:00; `_tick_ts` cae después — el filtro compara la hora
  equivocada (H1, mecanismo preciso).
- TDD: 2 tests nuevos en `tests/analysis/test_ciclos_faulty.py` reproducen el
  bug exacto (rojo confirmado antes del fix).
- Fix: `ciclos.py` línea de `hms` ahora usa `_seconds_of_day(_tick_ts)`.
  Citas file:line de `borde_del_dia.py`/`test_borde_del_dia.py` actualizadas
  (213-214→225-226, 242→254) por desplazamiento de líneas, sin cambio de
  sustancia.
- `pytest tests/analysis -q` → 307 passed (305 baseline + 2 nuevos), 0 rojos.
- Commit `492556b` (fix + tests + citas).
- Backups `.bak-20260816T004948Z` de los 9 artefactos de datos ANTES de re-correr
  (4 gitignored bajo `data/`, 5 trackeados bajo `research/`).
- Re-corrida completa (`llamador.py` → `comparador.py` → `divergencia_neto.py`) hecha.
  git_sha `0198002daee7a6469edd81d326d83ccfc3c1f175`.
- Detectada y verificada contaminación con commit concurrente `f3dda1a` (T0.7-M-E,
  bound Ticks.first_at a 60s) que aterrizó entre la última corrida de T0.7-M-D y esta
  tarea. Verificado puntualmente (script ad-hoc, `Ticks.first_at` con tolerance_s=60.0
  vs 1e18) que es INERTE para los 3 instantes de los 5 casos del brief.
- Resultado: 5/5 posiciones tempranas movidas a 18:45 (Δ 0.0-2.19s), 9/9 ya-correctas
  sin cambio exacto, 2/2 casos raros fuera de alcance sin cambio, 1 no-emparejado
  (55216338) quedó emparejado como efecto colateral (mejora no pedida).
- `tests/research/test_baseline_parity.py -m slow`: 3 failed, 1 passed — RED BY DESIGN,
  sin tocar, igual que antes.
- Reporte final escrito: `T0.7-M-G-reporte.md`. Pendiente: commit de resultados.
