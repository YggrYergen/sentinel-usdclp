# T0.7-M-E — Progreso

Brief: `research/fases/F0-preparacion/02-specs/T0.7-M-E-brief-fix-lookahead-motor-largo.md`

## Bloque 1 — lectura + gate ANTES (2026-08-15)

- Leidos: CHARTER.md, TRACKER.md (estado en una linea), el brief completo,
  `scripts/analysis/realtick_bt/backtest.py` (1-160, 330-390),
  `tests/analysis/test_lookahead_harness.py` (completo),
  `scripts/research/backtest_largo_ava.py` (completo),
  `scripts/analysis/realtick_bt/auditoria_lookahead.py` (completo, NO se modifica).
- **NO leido** `scripts/analysis/realtick_bt/faulty/` (frontera dura de la tarea, ver mensaje del
  despachador). El brief (E.4) sugiere leer `faulty/ciclos.py` commit `34152d1` como plantilla de
  estilo; se omite deliberadamente por la frontera y se implementa directo desde el spec cerrado
  (E.1-E.3), que ya trae todos los valores necesarios.
- **E.2 verificado por lectura:** `AvaTicks(bt.Ticks)` en `scripts/research/backtest_largo_ava.py:274-315`
  sobrescribe UNICAMENTE `_load`. `__init__` (linea 282) llama `super().__init__()` **sin argumentos**
  (confirma la restriccion 🔴 del brief: el parametro de tolerancia DEBE ser keyword-with-default).
  `first_at`, `range`, `_candidates` no aparecen en la clase: heredados byte-identicos.
- **Parity gate ANTES** (sin tocar codigo aun):
  `python -m pytest tests/research/test_baseline_parity.py -m slow -q` → `4 passed in 2.53s`.

## Bloque 2 — TDD rojo -> verde + fix regresion de linea (2026-08-15)

- **TDD rojo:** actualizados en `tests/analysis/test_lookahead_harness.py` los 3 tests que
  afirmaban el comportamiento SIN cota (`..._salta_un_hueco_artificial_sin_cota`,
  `..._cruza_fichero_de_mes_sin_cota`, `..._no_comprueba_distancia_devuelve_precio_tal_cual`) para
  afirmar `None` bajo la tolerancia default; cada uno se acompaño de un test hermano
  `..._con_tolerancia_grande_reproduce_comportamiento_viejo` que pasa `tolerance_s` explicito
  grande y reproduce el comportamiento exacto de antes (misma aserción). El cuarto test
  (`..._devuelve_none_si_no_hay_tick_posterior_en_los_3_meses`) no cambia: su comportamiento es
  independiente de la tolerancia. Añadidos: `test_first_at_dentro_de_tolerancia_default_devuelve_precio_tal_cual`,
  `test_first_at_limite_exacto_de_tolerancia_no_es_none` (frontera `==tolerance` no excluye), y
  `test_ticks_init_sin_argumentos_tolerancia_default_es_60` (E.1). `_synthetic_ticks()` ganó un
  parametro opcional `tolerance_s`.
  Confirmado ROJO: `python -m pytest tests/analysis/test_lookahead_harness.py -q` →
  `8 failed, 14 passed in 1.40s` (los 8 fallos son exactamente los tests nuevos/modificados;
  `TypeError: Ticks.__init__() got an unexpected keyword argument 'tolerance_s'` y
  `AttributeError: 'Ticks' object has no attribute 'tolerance_s'`).
- **Implementacion minima** en `scripts/analysis/realtick_bt/backtest.py`:
  - `Ticks.__init__(self, *, tolerance_s: float = 60.0)` (keyword-with-default, per E.1 🔴).
  - `first_at`: tras encontrar el primer tick `i` con `t >= t_sec` en el mes candidato mas
    temprano, si `t_tick - t_sec > self.tolerance_s` devuelve `None` en vez del tick; si no,
    devuelve el tick tal cual (comportamiento sano sin cambios). No se toco `_candidates`, `range`
    ni `resolve()` (ambos call sites ya manejaban `None`, exactamente como predice E.1).
  - No se emite ningun evento/log (backtest.py no tiene uno; E.4 lo prohibe explicitamente).
- Confirmado VERDE: `python -m pytest tests/analysis/test_lookahead_harness.py -q` →
  `22 passed in 1.52s`.
- **Regresion detectada y corregida (fuera de la lista de ficheros del brief, pero necesaria para
  el DoD "sin regresiones"):** `python -m pytest tests/analysis -q` dio
  `1 failed, 293 passed in 52.51s` -- `tests/analysis/test_borde_del_dia.py::test_cita_gate_spread_harness_verificada_contra_el_fichero_real`
  citaba `backtest.py` linea 358 (indice 357) en dur para `abs(sp - 0.5) <= 0.05`; mi cambio
  inserto 18 lineas antes de `resolve()` (8 en `__init__`, 10 en `first_at`, ver diff), desplazando
  esa linea a 376 (indice 375). Corregido el indice hardcodeado en el test (no se toco
  `scripts/analysis/realtick_bt/faulty/` -- fuera de frontera -- ni el modulo `M` que ese test
  importa desde alli).
  🟡 **Dato declarado, no corregido (fuera de mi alcance):** la constante
  `M.CITA_GATE_SPREAD_HARNESS["lineas"] == "349-359"` (definida en
  `scripts/analysis/realtick_bt/faulty/borde_del_dia.py`, bajo la frontera dura de esta tarea)
  queda ahora desactualizada en +18 lineas respecto al fichero real (`resolve()` esta ahora en
  354-364 aprox., no 336-346). El test que la usa (`test_cita_gate_spread_harness_vivo_backtest_py_literal`)
  solo comprueba la constante contra si misma, no contra el fichero, asi que sigue en verde -- pero
  la cita ya no es precisa. No se toca por ser territorio `faulty/`.
- Confirmado VERDE, suite completa: `python -m pytest tests/analysis -q` →
  `294 passed in 53.24s` (288 baseline + 6 tests nuevos de este bloque).
- **Commit de este bloque:** ver `git log` (mensaje describe que tests cambiaron y por que).

## Bloque 3 — parity gate DESPUES (2026-08-15)

`python -m pytest tests/research/test_baseline_parity.py -m slow -q` → **`3 failed, 1 passed in 2.42s`**
(ROJO esperado por diseno -- el look-ahead vivia dentro de la linea base congelada). Primera
discrepancia nombrada por el propio test (orden de ejecucion pytest-parametrize: S6 primero):

```
AssertionError: S6-K2P0: numero de posiciones cambio -- congelado=633 actual=624. Una
modificacion de motor altero una estrategia VIVA: PARAR y escalar (R1-bis, charter SS A.11).
assert 624 == 633
```

Las otras dos: `S7-TPNONE` numero de posiciones cambio, congelado=717 actual=708 (mismo tipo de
fallo que S6). `SuperTrend-p14x3-M15` SI paso el check de longitud (mismo numero de posiciones)
pero la posicion #102 difiere en contenido: `entry_delay_bars` 0->1, `entry_fill`
4768.84->4770.43, `t_in_exec` 1775754000.0->1775758500.0 (+4500s = 5 barras M15),
`margin1`/`net1` recalculados en consecuencia (la entrada real que antes usaba un tick de
look-ahead ahora se resuelve 5 barras despues, con precio distinto).
No se re-congela la linea base (prohibido); no se anade skip/xfail. NO se toco nada bajo
`04-resultados/T0.6-baseline/`.

## Bloque 4 — E.5, verificacion cuantitativa (2026-08-15)

- **Respaldo antes de re-medir:** `auditoria_lookahead_ava_q6.csv` (T0.7-M-C, PRE-fix) copiado a
  `auditoria_lookahead_ava_q6.csv.bak-20260815T204419Z` ANTES de invocar `medir_q6_ava()` (que lo
  sobrescribe como efecto colateral documentado en su propio codigo). `auditoria_lookahead.json`/
  `.csv` (Q4/Q5) NO se tocaron -- no se invoco `auditoria_lookahead.main()`, solo
  `medir_q6_ava()` y funciones puras, directamente.
- **Sensibilidad PRE-fix** (15/60/300s) recomputada del backup: `n_gt_15s=1122`, `n_gt_60s=945`
  (coincide con el valor citado en el brief), `n_gt_300s=794`.
- **Medicion POST-fix:** `auditoria_lookahead.medir_q6_ava(modo="mediana")` ejecutada tras el
  commit `f3dda1a`. Tardo 234,16s (build_all: 116,3s). Resultado completo en
  `fix_lookahead_harness.json`. Resumen:
  - `n_gt_60s`: 945 -> **0** (CUMPLE el criterio del brief, exacto).
  - `p50`: 0,108s -> 0,097s (−10,2% relativo).
  - `p90`: 3,268s -> 0,663s (**−79,7% relativo**).
  - Llamadas genuinas: 13.672 -> 13.824 (+152); llamadas que ahora devuelven `None`: 0 -> 994.
  - Posiciones por estrategia vs `F0-BT-LARGO-0001` (4.578/5.364/1.617): S6 4.482 (−96, −2,10%),
    S7 5.253 (−111, −2,07%), SuperTrend 1.566 (−51, −3,15%).
- 🔴 **CONCERN, no resuelto por mi (rol IMPLEMENTADOR, no interpreto):** el brief dice textualmente
  que si p50/p90 "move appreciably... you must STOP and escalate". p50 se mantiene en el mismo
  orden de magnitud; **p90 se movio 79,7% relativo, que no luce "esencialmente sin mover"**. Se
  declara la cifra medida y el dato factual acompañante (la poblacion de llamadas evaluables
  cambio: +152 llamadas totales, +994 ahora `None`) SIN decidir si esto es aceptable o si requiere
  accion -- eso corresponde al user/controlador. Marcado en el reporte final como "concern".
- Artefactos escritos: `fix_lookahead_harness.json`, `fix_lookahead_harness.md` (este directorio de
  02-specs referencia sus rutas completas en el reporte final).
- Parity gate DESPUES ya corrido en el Bloque 3 (rojo esperado, 3 failed 1 passed). No se repite.

## Siguiente paso (para retomar en frio si hace falta)

Todo el trabajo tecnico de T0.7-M-E esta HECHO: fix implementado y commiteado (`f3dda1a`), TDD
rojo->verde documentado, `tests/analysis -q` sin regresiones (294 passed), parity gate antes/despues
capturado, E.5 medido y escrito en `fix_lookahead_harness.{json,md}`. Falta: (a) commitear los
artefactos de E.5 + este fichero de progreso + `auditoria_lookahead_ava_q6.csv` (modificado) y su
`.bak`, con `git add -- <rutas>` (NO `-A`, NO `.`); (b) escribir el reporte final en
`02-specs/T0.7-M-E-reporte.md` citando exactamente lo de arriba, incluyendo el concern del p90
como "lo que no pude decidir/hacer" (no es un STOP total: el fix esta implementado, probado y
medido; el concern es sobre si el criterio de "p90 sin mover" del brief se cumple).
