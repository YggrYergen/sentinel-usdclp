# T0.7-M-H — progreso (una línea por bloque, escrita antes del commit)

- **Bloque 0 (recon + baseline pytest):** generador confirmado
  `scripts/analysis/realtick_bt/faulty/divergencia_neto.py` (721 líneas, pandas puro, no importa
  el motor); entradas `data/analysis/p_cap/replica/posiciones_replica.csv` (157 filas) y
  `data/analysis/p_cap/comparacion_p_cap.csv` (159 filas: 152 REAL + 7 REPLICA_SIN_PAREJA, no 162
  como decía el brief — difiere del brief, gana el CSV); salidas actuales
  `04-resultados/T0.7-p-cap/divergencia_neto.{csv,json,md}`; datos de fill
  `04-resultados/T0.7-p-cap/fill_vs_cotizacion.csv` (304 filas, generado por
  `fill_vs_cotizacion.py`, `t_epoch` = epoch en segundos hora servidor, sin conversión). Baseline
  pytest `python -m pytest tests/analysis -q`: **1 failed, 306 passed en 54.48s** — el failed es
  `test_borde_del_dia.py::test_cita_gate_spread_harness_verificada_contra_el_fichero_real`, cita
  `backtest.py:376` (ruta del otro agente en paralelo, no tocada por mí; falla pre-existente,
  ajena a este brief).
- **Bloque 1 (calibración del coste):** `calibrar_coste_deslizamiento()` en `divergencia_neto.py`
  recalcula desde `fill_vs_cotizacion.csv` con `mediana(|delta_vigente|)`: stops (`reason_name=='SL'`)
  0,255 @spread 0,50 (n=110) y 0,270 @spread 0,60 (n=9) — **coinciden** con D-54; mercado
  (`reason_name=='EXPERT'`, n=21) da **0,03** (mediana), no 0,053 — D-54's 0,053 coincide con la
  MEDIA de esa misma serie (0,0529), no la mediana; declarado como discrepancia, **gana el
  recálculo** (mediana, por instrucción explícita del brief). Coste resultante: 0,225 @0,50 y 0,240
  @0,60. Spread no recuperable por posición ni en `posiciones_replica.csv` ni en
  `comparacion_p_cap.csv` (verificado, ninguna columna 'spread') → modo dominante 0,50 (110/119 de
  los stops), coste aplicado a la réplica = **0,225**, para el 100 % de sus cierres por stop
  (spread nunca recuperable por posición en la réplica).
- **Bloque 2 (aplicación + flag):** flag `--coste-deslizamiento` añadido a `divergencia_neto.py`,
  apagado por defecto; verificado por código (`json.load` + diff campo a campo excluyendo
  `lineage.git_sha`/`lineage.timestamp`, y `diff` binario de los CSV) que sin el flag el script
  reproduce EXACTAMENTE `divergencia_neto.{csv,json,md}` de antes del cambio — CSV byte-idéntico,
  JSON idéntico campo a campo, MD idéntico salvo la línea de `git_sha`. Con el flag: nuevos
  `divergencia_neto_con_deslizamiento.{csv,json,md}`, coste aplicado sólo a
  `replica_motivo_cierre=='SL'` / `motivo_cierre=='SL'` (LONG: `precio_close -= 0.225`, SHORT:
  `precio_close += 0.225`; verificado en 2 posiciones ejemplo, delta exacto ±0,225, no-stop delta
  0,0). `python -m pytest tests/analysis -q`: **1 failed, 306 passed en 56.18s** — mismo failed
  pre-existente ajeno (`backtest.py`), sin regresiones frente al Bloque 0.
- **Bloque 3 (los tres números + control):** calculados por código dentro de
  `_ejecutar_con_deslizamiento()`, ver `divergencia_neto_con_deslizamiento.json` claves
  `tres_numeros_bloque_3` y `control_coste_uniforme_todas_posiciones`; cifras en crudo en el
  reporte final `T0.7-M-H-reporte.md`.
