# T0.7-M-H — progreso (una línea por bloque, escrita antes del commit)

- **Bloque 0 (recon + baseline pytest):** generador confirmado
  `scripts/analysis/realtick_bt/faulty/divergencia_neto.py` (721 líneas, pandas puro, no importa
  el motor); entradas `data/analysis/p_cap/replica/posiciones_replica.csv` (157 filas) y
  `data/analysis/p_cap/comparacion_p_cap.csv` (159 filas: 152 REAL + 7 REPLICA_SIN_PAREJA, no 162
  como decía el brief — difiere del brief, gana el CSV); salidas actuales
  `04-resultados/T0.7-p-cap/divergencia_neto.{csv,json,md}`; datos de fill
  `04-resultados/T0.7-p-cap/fill_vs_cotizacion.csv` (304 filas, generado por
  `fill_vs_cotizacion.py`, `t_epoch` = epoch en segundos hora servidor, sin conversión). Baseline
  pytest `python -m pytest tests/analysis -q`: **1 failed, 306 passed en 54.48s** — el failed es
  `test_borde_del_dia.py::test_cita_gate_spread_harness_verificada_contra_el_fichero_real`, cita
  `backtest.py:376` (ruta del otro agente en paralelo, no tocada por mí; falla pre-existente,
  ajena a este brief).
