# T0.7-M-E — Reporte final

Brief: `research/fases/F0-preparacion/02-specs/T0.7-M-E-brief-fix-lookahead-motor-largo.md`
Progreso detallado (4 bloques, resumible por un agente en frio): `research/fases/F0-preparacion/02-specs/T0.7-M-E-progreso.md`

## 1. Rutas de los artefactos producidos

- `scripts/analysis/realtick_bt/backtest.py` — `Ticks.__init__` (kwarg `tolerance_s: float = 60.0`,
  keyword-with-default) y `Ticks.first_at` (retorna `None` si `t_tick - t_sec > self.tolerance_s`),
  líneas 83-149 tras el cambio.
- `tests/analysis/test_lookahead_harness.py` — 3 tests que afirmaban el comportamiento SIN cota
  actualizados para afirmar `None` bajo la tolerancia default, cada uno con un test hermano que
  reproduce el comportamiento viejo pasando `tolerance_s` grande; 1 test sin cambios (independiente
  de la tolerancia); 4 tests nuevos (contrato `tolerance_s`, frontera exacta, camino sano,
  comportamiento viejo alcanzable). `_synthetic_ticks()` ganó parámetro opcional `tolerance_s`.
- `tests/analysis/test_borde_del_dia.py` — 1 línea corregida (índice `357` → `375`): cita hardcodeada
  que apuntaba a `abs(sp - 0.5) <= 0.05` en `backtest.py`, desplazada por las 18 líneas que el fix
  insertó antes de `resolve()`. No se tocó `scripts/analysis/realtick_bt/faulty/` ni el módulo `M`
  que ese test importa de ahí.
- `research/fases/F0-preparacion/04-resultados/T0.7-p-cap/fix_lookahead_harness.json` y `.md` —
  verificación cuantitativa E.5 (tabla antes/después, sensibilidad de umbral, delta de posiciones).
- `research/fases/F0-preparacion/04-resultados/T0.7-p-cap/auditoria_lookahead_ava_q6.csv` —
  sobrescrito por la re-ejecución de `medir_q6_ava()` con el fix (efecto colateral documentado de
  esa función, permitido por E.4: "may import or invoke it"). Estado PRE-fix respaldado en
  `auditoria_lookahead_ava_q6.csv.bak-20260815T204419Z` (mismo directorio) antes de sobrescribir.
- `auditoria_lookahead.json` / `auditoria_lookahead.csv` (Q4/Q5, propiedad de T0.7-M-C): **NO
  tocados** — no se invocó `auditoria_lookahead.main()`.
- `research/fases/F0-preparacion/02-specs/T0.7-M-E-progreso.md` — log de progreso, 4 bloques.
- No se escribió en `research/LEDGER.jsonl` ni `research/TRACKER.md` (ficheros del controlador).

### Commits (rutas propias únicamente, `git add -- <rutas>` / `git commit -m "<msg>" -- <rutas>`)

- `f3dda1aff0f84ae739b09a467da4c0bffe0252c4` — fix + TDD (backtest.py, test_lookahead_harness.py,
  test_borde_del_dia.py, T0.7-M-E-progreso.md).
- `9a0eb04` — artefactos E.5 (fix_lookahead_harness.{json,md}, auditoria_lookahead_ava_q6.csv +
  `.bak`, T0.7-M-E-progreso.md).

## 2. Comandos ejecutados y su salida real

### 2.1 Lectura previa (sin ejecución)

Leídos íntegramente: `research/CHARTER.md`, `research/TRACKER.md` (bloque "estado en una línea"),
el brief completo, `backtest.py` (líneas 1-160 y 330-390), `tests/analysis/test_lookahead_harness.py`,
`scripts/research/backtest_largo_ava.py` (completo), `scripts/analysis/realtick_bt/auditoria_lookahead.py`
(completo, no modificado). **No leído** `scripts/analysis/realtick_bt/faulty/` (frontera dura del
despacho; E.4 del brief sugiere leer `faulty/ciclos.py` como plantilla de estilo, se omitió
deliberadamente por la frontera — el spec cerrado ya trae todos los valores necesarios).

**E.2 verificado por lectura:** `AvaTicks(bt.Ticks)` (`scripts/research/backtest_largo_ava.py:274-315`)
sobrescribe únicamente `_load`. `__init__` (línea 282-286) llama `super().__init__()` sin
argumentos — confirma la restricción del brief de que `tolerance_s` debe ser keyword-with-default.
`first_at`, `range`, `_candidates` no aparecen en la clase: heredados byte-idénticos.

### 2.2 Parity gate ANTES del fix

```
$ python -m pytest tests/research/test_baseline_parity.py -m slow -q
....                                                                     [100%]
4 passed in 2.53s
```

### 2.3 TDD — rojo

```
$ python -m pytest tests/analysis/test_lookahead_harness.py -q
[...]
FAILED tests/analysis/test_lookahead_harness.py::test_first_at_hueco_artificial_devuelve_none_bajo_tolerancia_default
FAILED tests/analysis/test_lookahead_harness.py::test_first_at_hueco_artificial_con_tolerancia_grande_reproduce_comportamiento_viejo
FAILED tests/analysis/test_lookahead_harness.py::test_first_at_cruza_fichero_de_mes_pero_none_bajo_tolerancia_default
FAILED tests/analysis/test_lookahead_harness.py::test_first_at_cruza_fichero_de_mes_con_tolerancia_grande_reproduce_comportamiento_viejo
FAILED tests/analysis/test_lookahead_harness.py::test_first_at_comprueba_distancia_none_si_excede_tolerancia_default
FAILED tests/analysis/test_lookahead_harness.py::test_first_at_limite_exacto_de_tolerancia_no_es_none
FAILED tests/analysis/test_lookahead_harness.py::test_first_at_con_tolerancia_grande_reproduce_comportamiento_viejo
FAILED tests/analysis/test_lookahead_harness.py::test_ticks_init_sin_argumentos_tolerancia_default_es_60
8 failed, 14 passed in 1.40s
```
Causas de fallo reales (`TypeError: Ticks.__init__() got an unexpected keyword argument 'tolerance_s'`,
`AttributeError: 'Ticks' object has no attribute 'tolerance_s'`) — confirman que el rojo es por
ausencia de la implementación, no por error de test.

### 2.4 Implementación mínima — verde

```
$ python -m pytest tests/analysis/test_lookahead_harness.py -q
......................                                                   [100%]
22 passed in 1.52s
```

### 2.5 Suite dirigida — regresión detectada y corregida

```
$ python -m pytest tests/analysis -q
...................................................F.................... [ 24%]
[...]
FAILED tests/analysis/test_borde_del_dia.py::test_cita_gate_spread_harness_verificada_contra_el_fichero_real
1 failed, 293 passed in 52.51s
```
Causa: cita hardcodeada de número de línea (357→375) desplazada por las 18 líneas insertadas antes
de `resolve()`. Corregida solo en `tests/analysis/test_borde_del_dia.py` (no se tocó `faulty/`).

```
$ python -m pytest tests/analysis -q
........................................................................ [ 24%]
........................................................................ [ 48%]
........................................................................ [ 73%]
........................................................................ [ 97%]
......                                                                   [100%]
294 passed in 53.24s
```
(288 baseline + 6 tests nuevos de `test_lookahead_harness.py`, 0 fallos.)

### 2.6 Parity gate DESPUÉS del fix

```
$ python -m pytest tests/research/test_baseline_parity.py -m slow -q
.FFF                                                                     [100%]
3 failed, 1 passed in 2.42s
```
Primera discrepancia nombrada por el propio test:
```
AssertionError: S6-K2P0: numero de posiciones cambio -- congelado=633 actual=624. Una
modificacion de motor altero una estrategia VIVA: PARAR y escalar (R1-bis, charter SS A.11).
assert 624 == 633
```
Las otras dos: `S7-TPNONE` congelado=717 actual=708 (mismo tipo de fallo, longitud distinta);
`SuperTrend-p14x3-M15` mismo número de posiciones pero la posición #102 difiere:
`{'entry_delay_bars': (0, 1), 'entry_fill': (4768.84, 4770.43), 'margin1': (4466018.66, 4467507.695),
'net1': (-507583.0000000068, -656486.5000000205), 't_in_exec': (1775754000.0, 1775758500.0)}`.
No se re-congeló la línea base, no se agregó `skip`/`xfail`, no se tocó `04-resultados/T0.6-baseline/`.

### 2.7 E.5 — verificación cuantitativa (Q6 AVA, llamadas genuinas)

Backup antes de re-medir (`medir_q6_ava()` sobrescribe su propio CSV):
```
$ cp auditoria_lookahead_ava_q6.csv auditoria_lookahead_ava_q6.csv.bak-20260815T204419Z
```

Sensibilidad PRE-fix (recomputada del backup, columna `delta_s`, filas `evaluable=True`):
```
n_evaluable_pre_fix 13672 of 13672
n_gt_15.0s 1122
n_gt_60.0s 945
n_gt_300.0s 794
p50 0.10800004005432129
p90 3.2682999134063624
p99 23400.06544007063
max 76129.38899993896
```
(coincide con la tabla del brief: p50 0.108, p90 3.268, n_gt_60s 945, max 76129.39)

Medición POST-fix (`al.medir_q6_ava(modo="mediana")`, tras commit `f3dda1a`):
```
elapsed 234.16110038757324
{
  "modo": "mediana",
  "n_bars_final": 31700,
  "n_posiciones_por_estrategia": {
    "S6-K2P0": 4482,
    "S7-TPNONE": 5253,
    "SuperTrend-p14x3-M15": 1566
  },
  "n_llamadas_first_at": 13824,
  "tiempo_medido_seg": 116.3,
  "resumen_delays": {
    "n_total": 13824,
    "n_evaluable": 12830,
    "n_no_evaluable": 994,
    "p50": 0.0969998836517334,
    "p90": 0.6632000923156746,
    "p99": 21.71371000766754,
    "max": 59.92199993133545,
    "n_gt_60s": 0,
    "n_gt_15min": 0,
    "n_gt_1h": 0
  },
  "n_llamadas_dentro_de_exclusion_ava": 1,
  "n_intervalos_exclusion_ava": 44
}
```
Sensibilidad POST-fix (recomputada del CSV post-fix): `n_gt_15s=189`, `n_gt_60s=0`, `n_gt_300s=0`.

Delta de posiciones vs `F0-BT-LARGO-0001` (4.578 / 5.364 / 1.617): S6 −96 (−2,10%), S7 −111
(−2,07%), SuperTrend −51 (−3,15%). Llamadas ahora `None`: 0 → 994.

**Criterio `n_gt_60s`:** 945 → 0. Cumple, exacto.
**Criterio p50/p90 "essentially unchanged":** p50 −10,2% relativo; **p90 −79,7% relativo**. Ver
sección 3.

Todos los números arriba, con más detalle y las fuentes exactas de cada uno, están en
`fix_lookahead_harness.json` / `.md`.

## 3. Lo que NO pude hacer, y por qué

- **No decidí si el movimiento de p90 (3,268s → 0,663s, −79,7% relativo) es aceptable.** El brief
  dice textualmente: *"p50 and p90 must stay essentially unchanged... If they move appreciably,
  the fix is touching healthy calls and you must STOP and escalate."* p50 se mueve poco (−10,2%);
  p90 se mueve 79,7%. Mi rol es IMPLEMENTADOR/reporte objetivo (charter §B): no interpreto si esto
  es "appreciably" ni propongo causa ni arreglo. Dato factual medido y no interpretado: la
  población de llamadas "evaluables" sobre la que se calcula p90 cambió de composición entre antes
  y después (13.672 → 13.824 llamadas totales; 0 → 994 ahora devuelven `None`), no solo se acotó
  el mismo conjunto de valores. Esto queda declarado en `fix_lookahead_harness.json` /`.md` para
  que el user/controlador decida si corresponde STOP-y-escalar formal.
- **La paridad bit-idéntica sigue sin alcanzarse** — esto es el resultado *esperado* de la tarea
  (E.5 lo predice y el brief lo declara "expected, not a failure"), no algo que no pude hacer.
- No se recorrió `F0-BT-LARGO-0001` completo (métricas net/PF/maxDD por estrategia) con el fix —
  fuera del alcance de E.5, que solo pide `n_gt_X`, p50/p90, sensibilidad y delta de conteo de
  posiciones.
- No se tocó `scripts/analysis/a6_pata_a/signal_level.py` (T0.7-M-F, tarea separada) ni
  `scripts/analysis/realtick_bt/faulty/` (frontera dura del despacho).
- No se escribió en `research/LEDGER.jsonl` ni `research/TRACKER.md` (ficheros del controlador,
  fuera de mi alcance por instrucción explícita).
