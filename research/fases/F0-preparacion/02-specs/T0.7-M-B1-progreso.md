# T0.7-M-B1 -- bitácora de progreso (borde del día)

Estado: **HECHO.** Las 7 preguntas del brief están respondidas en
`research/fases/F0-preparacion/04-resultados/T0.7-p-cap/borde_del_dia.md`
(+ `.csv` + `.json`), generadas por
`scripts/analysis/realtick_bt/faulty/borde_del_dia.py`, con
`tests/analysis/test_borde_del_dia.py` (31 tests) en verde.

## Qué se hizo, en orden

1. Leído el brief, CHARTER.md, TRACKER.md (líneas 1-270) y NEGATIVOS.md.
2. Identificado el cluster del borde del día sobre `censo_cola_t_open.csv`
   (39 filas, ya hecho, no re-derivado): 9 filas `REPLICA_TEMPRANO` con
   `6000 <= |delta_t_open_s| <= 6500` (grupo "principal") + la fila 0 (ya
   viene primera: el CSV está pre-ordenado por `-abs(delta_t_open_s)`,
   verificado `is_monotonic_decreasing`) como grupo "fin_de_semana" -- 10
   casos en total, 7 días distintos por el lado réplica.
3. Localizado el mecanismo (pregunta 1+3): `ciclos.py:161`
   (`ticks.first_at(t)`, primer tick con `t >= T`, NO el último `<= T`) más
   `ciclos.py:211` (`"t_open": t` -- guarda el instante del bucle, no el
   timestamp del tick encontrado). En los 10 casos el segundo `T` no tiene
   ningún tick (hueco de mantenimiento), el tick vigente (`<=T`) tiene
   spread 0,60 (supera el umbral), y el primer tick futuro (`>=T`, el que
   de verdad usa el código) llega ~3650-3665 s después (o ~176.699 s en el
   caso fin de semana) con spread exactamente 0,50 (pasa el gate).
4. `llamador.py:474-478` declara explícitamente que los huecos de fin de
   semana NO reciben tratamiento especial. Grep negativo confirmado sobre
   `estado_por_barra.py`, `ciclos.py`, `config_faulty.py`, `llamador.py`,
   `comparador.py` (patrón `17:00|maint|weekend|fin_de_semana|domingo|
   saturday|sunday|session|sesion|blocked|gate`): sin más resultados que
   los ya citados.
5. Pregunta 4: ticks por minuto, ventana 16:45→18:50 servidor, 7 días.
   Hueco de 61 min (16:59-17:59) en 6 de 7 días; el día del cluster
   fin-de-semana (2026-07-31, viernes) tiene hueco de 115 min (16:55-18:49).
6. Pregunta 5: barra M15 decisoria vía la misma fórmula que
   `ciclos.py::correr_ciclos` paso 1. 9/10 casos usan la barra 16:30-16:45
   (completa, contigua). 1 caso (position_id 55242134) usa la barra
   16:45-17:00, que NO es contigua con la siguiente del parquet (salta a
   18:00: hueco de 3600 s, el rollover de `F0-DATA-CAP-0001`).
7. Pregunta 6: las 16 posiciones `OPEN_SKIPPED_SL_CROSSED`, con
   `delta_t_open_s` con signo, `delta_precio_open`, evento de la réplica
   en `real_t_open_epoch` (±1 s), y `desired_sl` extraído del propio
   `detalle` JSON de los eventos `OPEN_SKIPPED_SL_CROSSED` en el intervalo
   (no de `sl_vivo` de `posiciones_replica.csv`: ninguno de los 16 tiene
   fila propia ahí porque la posición nunca se creó -- declarado no
   evaluable por esa vía, con la razón, en el `.md`).
8. Pregunta 7: 0 no-evaluables numéricos en preguntas 1-5 (los 10 instantes
   tienen tick vigente y tick futuro localizables, las 7 fechas tienen
   cobertura). En pregunta 6, `sl_vivo` declarado no evaluable por
   construcción (ver punto 7).
9. TDD: `tests/analysis/test_borde_del_dia.py` escrito primero, visto
   fallar (`ImportError`), implementado `borde_del_dia.py`, 5 tests
   fallaron por errores de expectativa en mi propio test (fórmula de barra
   vigente y bins de `contar_ticks_por_minuto`), corregidos, 31/31 verde.
10. `pytest tests/analysis -q` → **267 passed, 0 failed** (línea base del
    brief era 214; el excedente es de tareas en curso de otros agentes en
    esta sesión, no regresión: 0 failed).
11. `python scripts/analysis/realtick_bt/faulty/borde_del_dia.py` corrido
    una vez, produjo los 3 artefactos de datos.
12. Commit scoped a las 4 rutas del entregable + esta bitácora + (si aplica)
    el reporte final.

## Siguiente paso si esto se retoma en frío

Nada pendiente de este brief. Si se reabre: releer el `.md` de resultados
antes de tocar nada, no re-ejecutar `main()` sin respaldar los artefactos
existentes (`cp archivo archivo.bak-<timestamp>` primero).
