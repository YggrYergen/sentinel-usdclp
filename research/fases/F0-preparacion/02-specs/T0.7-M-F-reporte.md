# T0.7-M-F -- Reporte final

Brief: `research/fases/F0-preparacion/02-specs/T0.7-M-F-brief-fix-lookahead-ticksava.md`
Progreso (5 bloques): `research/fases/F0-preparacion/02-specs/T0.7-M-F-progreso.md`

## 1. Rutas de los artefactos producidos

- `scripts/analysis/a6_pata_a/signal_level.py` -- `TicksAva.__init__` (kwarg `tolerance_s: float =
  60.0`, keyword-only-con-default, línea 75) y `TicksAva.first_at` (retorna `None` si
  `t_tick - t_sec > self.tolerance_s`, línea 131). Clase `TicksAva` en línea 73, NO refactorizada a
  subclase de `bt.Ticks`. **Entra a control de versión por primera vez** con el commit `43785dc`
  (antes `??`, nunca commiteado).
- `tests/analysis/test_lookahead_ticksava.py` (nuevo) -- 11 tests, mirror de
  `tests/analysis/test_lookahead_harness.py` adaptado a `TicksAva`.
- `research/fases/F0-preparacion/04-resultados/T0.7-p-cap/fix_lookahead_ticksava.md` -- auditoría
  F.2 con citas `file:line` y salida real de los comandos.
- `research/fases/F0-preparacion/02-specs/T0.7-M-F-progreso.md` -- log de progreso, 5 bloques.
- **No se escribió en `research/LEDGER.jsonl` ni `research/TRACKER.md`** -- prohibición explícita
  del brief (§"Do NOT write to LEDGER.jsonl or TRACKER.md -- controller files"). El prompt de
  despacho pedía una fila de LEDGER; el brief (documento de mayor autoridad declarado por el propio
  prompt de despacho) lo prohíbe expresamente para esta tarea. Ver sección 3.

### Commits (rutas propias únicamente, `git add -- <rutas>` / `git commit -m "<msg>" -- <rutas>`)

- `43785dcd7dbc420b4c78a88d665683daea3fb75c` -- fix + TDD (`signal_level.py`,
  `test_lookahead_ticksava.py`, `T0.7-M-F-progreso.md`).
- `59ef5a0` -- artefacto F.2 (`fix_lookahead_ticksava.md`).
- (pendiente al cerrar este reporte) commit final de `T0.7-M-F-progreso.md` (bloque 5) +
  `T0.7-M-F-reporte.md`.

## 2. Comandos ejecutados y su salida real

### 2.1 Lectura previa (sin ejecución)

Leídos íntegros: `research/CHARTER.md`, `research/protocolos/00-inicio-de-sesion.md`, el brief
completo, `T0.7-M-E-reporte.md`, el diff completo del commit `f3dda1a` (plantilla), `signal_level.py`
completo (391 líneas antes del cambio), `backtest.py` líneas 300-414 (`run_supertrend`, `resolve`),
`capa1_senal_cruda.py`/`capa3_gate_spread.py`/`exp_active_fichas.py` (solo bloque de imports y línea
de instanciación de `Ticks`). No leído `scripts/analysis/realtick_bt/faulty/` (frontera dura).

**Verificación de líneas del brief:** `class TicksAva:` en `signal_level.py:73` y `first_at` en
`:125-133` -- confirmado exacto antes del cambio. `cap_ticks = Ticks()` / `ava_ticks = TicksAva()`
en `:298-299` -- confirmado exacto.

**F.2 verificado por lectura (auditoría completa en `fix_lookahead_ticksava.md`):**
`TicksAva.first_at` **no tiene ningún call site activo** en el código actual. El único consumidor
de `ava_ticks` es `st_signals(ava_bars_st, ava_ticks, gated=False)` (`signal_level.py:339`); dentro
de `st_signals`, `ticks` se pasa a `run_supertrend(bars, ticks)` (`backtest.py:306`), que solo llama
`ticks.range()` (`:331`), nunca `ticks.first_at()`. `resolve()` (`backtest.py:354`) -- la única
función que llama `.first_at()` en `:371`/`:401`, ambos ya manejando `None` -- solo se invoca desde
`st_signals` cuando `gated=True` (`:217`); la llamada con `ava_ticks` usa `gated=False` (`:339`).
No hubo que escalar por consumidor sin manejo de `None`: no existe consumidor activo del método.
Confirmado por lectura de imports: `capa1_senal_cruda.py:389`, `capa3_gate_spread.py:413`,
`exp_active_fichas.py:195`, `signal_level.py:298` instancian `Ticks` importado de
`scripts.analysis.realtick_bt.backtest` (ya acotado por `f3dda1a`), no `TicksAva`.

### 2.2 TDD -- rojo

```
$ python -m pytest tests/analysis/test_lookahead_ticksava.py -q
[...]
FAILED tests/analysis/test_lookahead_ticksava.py::test_first_at_hueco_artificial_devuelve_none_bajo_tolerancia_default
FAILED tests/analysis/test_lookahead_ticksava.py::test_first_at_hueco_artificial_con_tolerancia_grande_reproduce_comportamiento_viejo
FAILED tests/analysis/test_lookahead_ticksava.py::test_first_at_cruza_fichero_de_mes_pero_none_bajo_tolerancia_default
FAILED tests/analysis/test_lookahead_ticksava.py::test_first_at_cruza_fichero_de_mes_con_tolerancia_grande_reproduce_comportamiento_viejo
FAILED tests/analysis/test_lookahead_ticksava.py::test_first_at_comprueba_distancia_none_si_excede_tolerancia_default
FAILED tests/analysis/test_lookahead_ticksava.py::test_first_at_limite_exacto_de_tolerancia_no_es_none
FAILED tests/analysis/test_lookahead_ticksava.py::test_first_at_con_tolerancia_grande_reproduce_comportamiento_viejo
FAILED tests/analysis/test_lookahead_ticksava.py::test_ticksava_init_sin_argumentos_tolerancia_default_es_60
8 failed, 3 passed in 1.90s
```
Causas reales: `TypeError: TicksAva.__init__() got an unexpected keyword argument 'tolerance_s'`,
`AttributeError: 'TicksAva' object has no attribute 'tolerance_s'` -- confirman rojo por ausencia
de implementación, no error de test.

### 2.3 Implementación mínima -- verde

```
$ python -m pytest tests/analysis/test_lookahead_ticksava.py -q
...........                                                              [100%]
11 passed in 1.14s
```

### 2.4 Suite dirigida

```
$ python -m pytest tests/analysis -q
........................................................................ [ 23%]
........................................................................ [ 47%]
........................................................................ [ 70%]
........................................................................ [ 94%]
.................                                                        [100%]
305 passed in 55.41s
```
(294 baseline commit `404a125` + 11 tests nuevos, 0 fallos, 0 regresiones.)

### 2.5 Parity gate

```
$ python -m pytest tests/research/test_baseline_parity.py -m slow -q
.FFF                                                                     [100%]
3 failed, 1 passed in 2.72s
```
Mismos tres casos que el estado post-T0.7-M-E (ver `T0.7-M-E-reporte.md` §2.6), sin cuarto caso:
`S6-K2P0` congelado=633 actual=624; `S7-TPNONE` congelado=717 actual=708; `SuperTrend-p14x3-M15`
mismo conteo, posición `#102` con la misma diferencia exacta ya documentada
(`entry_delay_bars (0,1)`, `entry_fill (4768.84, 4770.43)`, `t_in_exec (1775754000.0, 1775758500.0)`,
etc.). No se re-congeló la línea base, no se agregó `skip`/`xfail`, no se tocó
`04-resultados/T0.6-baseline/`. La ausencia de un cuarto caso confirma por evidencia (no se asume)
que `TicksAva` no alimenta el path de paridad.

### 2.6 `git status --short` antes/después (filtrado a las rutas relevantes)

Antes:
```
?? scripts/analysis/a6_pata_a/barras_nativas_vs_derivadas.py
?? scripts/analysis/a6_pata_a/capa3_gate_spread.py
?? scripts/analysis/a6_pata_a/exp_active_fichas.py
?? scripts/analysis/a6_pata_a/fills_vs_feed_capitaria.py
?? scripts/analysis/a6_pata_a/signal_level.py
```
Después del commit `43785dc`:
```
?? scripts/analysis/a6_pata_a/barras_nativas_vs_derivadas.py
?? scripts/analysis/a6_pata_a/capa3_gate_spread.py
?? scripts/analysis/a6_pata_a/exp_active_fichas.py
?? scripts/analysis/a6_pata_a/fills_vs_feed_capitaria.py
```
`signal_level.py` deja de aparecer como `??` (entra a control de versión). Los otros cuatro
ficheros untracked del directorio (propiedad de otras tareas) permanecen intocados.

## 3. Lo que NO pude hacer, y por qué

- **No escribí fila en `research/LEDGER.jsonl`.** El prompt de despacho pedía explícitamente
  "Add the required LEDGER.jsonl row(s) per charter §13 con el REAL git_sha". El brief que ese
  mismo prompt declara como "tu especificación" prohíbe textualmente lo contrario: *"🔴 Do NOT
  write to `research/LEDGER.jsonl` or `research/TRACKER.md` -- controller files. Deliver the data;
  the controller writes the row and verifies it independently (charter §A.13: verifier ≠
  implementer)."* Seguí el brief (autoridad declarada superior por el propio prompt de despacho,
  salvo donde "the code wins") y NO escribí en LEDGER.jsonl ni TRACKER.md. Dejo los datos
  trazables en este reporte y en `fix_lookahead_ticksava.md` para que el controlador escriba la
  fila: `commit=43785dcd7dbc420b4c78a88d665683daea3fb75c`, `area=T0.7-M-F`,
  `experimento=fix-lookahead-ticksava`, artefactos arriba listados, `pytest tests/analysis -q` =
  305 passed, parity gate = 3 failed/1 passed (mismos tres casos, sin cuarto).
- No refactoricé `TicksAva` para heredar de `bt.Ticks` -- explícitamente fuera de alcance (F.1 del
  brief).
- No toqué `scripts/analysis/realtick_bt/backtest.py` ni `scripts/analysis/realtick_bt/faulty/`
  (fronteras duras del brief).
- No re-congelé la línea base de paridad ni añadí `skip`/`xfail` al test de paridad -- prohibido
  explícitamente.
- No pude escalar por un consumidor de `TicksAva.first_at` sin manejo de `None`, porque **no existe
  ningún consumidor activo** de ese método en el código actual (ver §2.1/F.2); no hay ambigüedad
  que reportar más allá de este hecho.
- Las líneas exactas citadas en el brief (`:73`, `:125-133`) se desplazaron ligeramente por los
  comentarios añadidos (`class TicksAva:` sigue en `:73`; `first_at` pasó de `:125` a `:131`).
  Ningún test existente (`test_borde_del_dia.py` u otro) cita líneas de `signal_level.py`, así que
  no hubo cita que romper.
